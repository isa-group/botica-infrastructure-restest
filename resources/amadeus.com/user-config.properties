# ====================================================================================================
# Configuration properties (rest of properties take default values indicated in config.properties)
# ====================================================================================================

# Path to the specification file
oas.path=/app/resources/amadeus.com/api-spec.yaml

# Path to the test configuration file
conf.path=/app/resources/amadeus.com/test-conf.yaml

# Path to the directory where the test cases will be generated
test.target.dir=/app/output/amadeus.com/test-cases

# Path to the directory where Allure will save the report
allure.results.dir=/app/output/amadeus.com/allure-results

# Path to the directory where Allure will save the report
allure.report.dir=/app/output/amadeus.com/allure-reports

# Experiment name (string used to identify the artefacts generated by RESTest in this execution)
experiment.name=amadeus

# Test case generation strategy (RT: Random Testing, CBT: Combinatorial Testing, ART: Adaptive Random Testing, FT: Fuzzing)
generator=CBT

# Ratio of faulty test cases to generate due to the violation of inter-parameter dependencies
faulty.dependency.ratio=0.4

# Number of test cases to be generated per operation
testsperoperation=10

# Number of the class containing the test cases to be generated
testclass.name=AmadeusTest
