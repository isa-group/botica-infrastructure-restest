You need to replace the string "CENSORED" with a valid authentication token.

In the "oauth.json" file, you need to replace the strings "CENSORED" with a valid client_id, client_secret's and refresh_token. These are obtained when creating a developer account in YouTube and registering an application.